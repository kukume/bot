ktor {
  deployment {
    port = 8080
    port = ${?PORT}
  }
  application {
    modules = [
      me.kuku.headless.ktor.ServerKt.config
      me.kuku.headless.controller.ExecControllerKt.exec
    ]
  }
}